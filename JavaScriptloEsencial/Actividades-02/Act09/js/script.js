/*
Definicion de funcion que division dos numeros
los cuales seon enviados como parametros
*/

function division(a, b) {
  return a / b;
}

var resultado = division(144, 12);

document.getElementById('resultado').innerHTML = resultado;
